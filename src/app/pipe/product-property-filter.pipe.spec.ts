import { ProductPropertyFilterPipe } from './product-property-filter.pipe';

describe('ProductPropertyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductPropertyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
